# Configuration file

aether_legacy {

    moa {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    phyg {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    flying_cow {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sheepuff {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    aerbunny {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    aerwhale {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.2

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.1

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=2.0
    }

    swet {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cockatrice {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sentry {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zephyr {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    aechor_plant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mimic {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    slider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    valkyrie_queen {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    fire_ball {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ice_ball {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    thunder_ball {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    valkyrie {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    fire_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mini_cloud {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    whirlwind {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


ancientbeasts {

    ghost {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zealot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vessel {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    fake_vessel {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    frostash_fox {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    frost_walker {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    giant_zombie {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lil_vessel {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    damcell {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    netherhound {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    boulderer {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    owlstack {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rifted_enderman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bonepile {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    nekros {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


ancientspellcraft {

    void_creeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton_mage_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton_mage {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wolf_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spirit_bear {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    fire_ant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton_horse_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ordinary_spider_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    entity_volcano {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spellcaster_entity {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    animated_item {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    creeper_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pig_zombie_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    stone_guardian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wizardmerchant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    evil_class_wizard {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


animania {

    mare_draft {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    stallion_draft {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    foal_draft {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hamster {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ferret_grey {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ferret_white {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hedgehog {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hedgehog_albino {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    piglet_yorkshire {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sow_yorkshire {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hog_yorkshire {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    piglet_old_spot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sow_old_spot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hog_old_spot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    piglet_large_black {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sow_large_black {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hog_large_black {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    piglet_large_white {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sow_large_white {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hog_large_white {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    piglet_hampshire {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sow_hampshire {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hog_hampshire {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    piglet_duroc {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sow_duroc {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hog_duroc {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    chick_leghorn {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hen_leghorn {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rooster_leghorn {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    chick_orpington {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hen_orpington {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rooster_orpington {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    chick_plymouth_rock {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hen_plymouth_rock {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rooster_plymouth_rock {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    chick_rhode_island_red {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hen_rhode_island_red {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rooster_rhode_island_red {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    chick_wyandotte {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hen_wyandotte {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rooster_wyandotte {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    calf_angus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cow_angus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bull_angus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    calf_friesian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cow_friesian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bull_friesian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    calf_hereford {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cow_hereford {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bull_hereford {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    calf_holstein {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cow_holstein {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bull_holstein {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    calf_longhorn {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cow_longhorn {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bull_longhorn {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    calf_highland {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cow_highland {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bull_highland {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    calf_jersey {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cow_jersey {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bull_jersey {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    calf_mooshroom {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cow_mooshroom {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bull_mooshroom {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toad {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    frog {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dartfrog {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kid_alpine {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_alpine {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_alpine {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kid_angora {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_angora {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_angora {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kid_fainting {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_fainting {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_fainting {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kid_kiko {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_kiko {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_kiko {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kid_kinder {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_kinder {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_kinder {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kid_nigerian_dwarf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_nigerian_dwarf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_nigerian_dwarf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kid_pygmy {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_pygmy {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_pygmy {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lamb_friesian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ewe_friesian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ram_friesian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lamb_suffolk {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ewe_suffolk {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ram_suffolk {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lamb_dorper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ewe_dorper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ram_dorper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lamb_dorset {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ewe_dorset {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ram_dorset {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lamb_merino {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ewe_merino {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ram_merino {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lamb_jacob {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ewe_jacob {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ram_jacob {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_cottontail {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_cottontail {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kit_cottontail {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_chinchilla {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_chinchilla {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kit_chinchilla {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_dutch {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_dutch {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kit_dutch {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_havana {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_havana {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kit_havana {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_jack {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_jack {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kit_jack {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_new_zealand {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_new_zealand {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kit_new_zealand {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_rex {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_rex {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kit_rex {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    buck_lop {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    doe_lop {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kit_lop {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peachick_charcoal {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peahen_charcoal {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peacock_charcoal {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peachick_opal {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peahen_opal {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peacock_opal {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peachick_peach {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peahen_peach {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peacock_peach {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peachick_purple {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peahen_purple {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peacock_purple {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peachick_taupe {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peahen_taupe {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peacock_taupe {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peachick_blue {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peahen_blue {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peacock_blue {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peachick_white {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peahen_white {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    peacock_white {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


arcaneworld {

    overgrown_sheep {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


artifacts {

    mimic {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


beneath {

    shadow {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


betteranimalsplus {

    brownbear {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    blackbear {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    deer {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lammergeier {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    feralwolf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    coyote {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    fox {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tarantula {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hirschgeist {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    goat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    jellyfish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pheasant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    reindeer {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    boar {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    squirrel {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    songbird {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    badger {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lamprey {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    nautilus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    crab {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    horseshoecrab {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    shark {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    moose {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    turkey {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zotzpyre {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bobbit_worm {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    goose {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    eel_freshwater {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    eel_saltwater {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    whale {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.5

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.5

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=1.0
    }

    walrus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kermodebear {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


betternether {

    firefly {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


betterslimes {

    blue_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    red_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    yellow_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    black_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    purple_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ice_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    jungle_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sand_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spectral_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    quazar {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.1

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    iron_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    gold_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    knight_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    haunted_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


betterwithaddons {

    karate_zombie {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


betterwithmods {

    entity_sheared_creeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bwm_jungle_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


biomesoplenty {

    wasp {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


butterflies {

    butterfly {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


charm {

    spectre {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


coroutil {

    bat_smart {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


defiledlands {

    shambler {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    shambler_twisted {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    scuttler {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    host {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    slime_defiled {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    book_wyrm {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    the_destroyer {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    the_mourner {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


dimdoors {

    mob_monolith {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


doggytalents {

    dog {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


dungeontactics {

    towerguardian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bone_wolf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    attacken {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


ebwizardry {

    zombie_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    husk_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    stray_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spider_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    blaze_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wither_skeleton_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    silverfish_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vex_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ice_wraith {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lightning_wraith {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spirit_wolf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spirit_horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    phoenix {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ice_giant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spectral_golem {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    magic_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    decoy {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    shadow_wraith {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    storm_elemental {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wizard {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    evil_wizard {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    remnant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


endreborn {

    endguard {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    watcher {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    endlord {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    chronologist {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


fairylights {

    ladder {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


futuremc {

    panda {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bee {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cod {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pufferfish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    salmon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tropical_fish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


golems {

    golem_bedrock {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_bone {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_bookshelf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_clay {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_coal {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_concrete {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_crafting {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_diamond {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_dispenser {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_emerald {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_end_stone {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_furnace {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_glass {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_glowstone {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_gold {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_hardened_clay {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_ice {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_lapis {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_leaves {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_magma {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_melon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_shroom {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_nether_brick {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_nether_wart {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_obsidian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_prismarine {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_quartz {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_red_sandstone {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_redstone {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_redstone_lamp {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_sandstone {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_sea_lantern {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_sponge {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_stained_clay {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_stained_glass {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_straw {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_tnt {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_wooden {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    golem_wool {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


harvestersnight {

    harvester {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.2

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


hordes {

    zombie_player {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


hunterillager {

    hunter_illager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


iceandfire {

    dragonegg {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dragonskull {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    firedragon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    icedragon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    snowvillager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hippogryph {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    stonestatue {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    gorgon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    if_pixie {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cyclops {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    siren {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hippocampus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    deathworm {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    if_cockatrice {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    stymphalianbird {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=2.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=0.8

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=0.8

        # The factor to scale the entity's max health by. Default: 1
        D:health=0.5

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.2

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.5
    }

    if_troll {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    myrmex_worker {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    myrmex_soldier {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    myrmex_sentinel {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    myrmex_royal {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    myrmex_queen {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    myrmex_egg {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    amphithere {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    seaserpent {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.2

        # The factor to scale the entity's speed by. Default: 1
        D:speed=0.9

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    myrmex_swarmer {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    if_mob_skull {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dread_thrall {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dread_ghoul {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dread_beast {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dread_scuttler {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dread_lich {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dread_knight {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dread_horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    if_hydra {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


iceologer {

    iceologer {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


lycanitesmobs {

    spriggan {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    arisaur {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vespidqueen {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    reiver {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lacedon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    barghest {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kobold {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    makaalpha {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    chupacabra {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cacodemon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    arix {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skylus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wendigo {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    djinn {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    eechetik {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    maug {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    crusk {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    uvaraptor {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    concapede {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ghoul {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    fear {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    amalgalich {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    joustalpha {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    astaroth {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bobeko {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    thresher {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    geist {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    eyewig {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    aglebemu {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    khalk {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    joust {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zoataur {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    asmodeus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    grell {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    nymph {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    gorgomite {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    serpix {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spectre {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vespid {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    warg {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cryptkeeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zephyr {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ettin {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    abtu {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    behemoth {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    morock {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dawon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    shambler {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lurker {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    afrit {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    jabberwock {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    beholder {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tremor {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    geonach {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lobber {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    troll {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    geken {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    strider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wraith {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    quillbeast {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    clink {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    archvile {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ika {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    herma {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vapula {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    treant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    reaper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    raiko {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    grue {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    silex {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    quetzodracl {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wildkin {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    aspid {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wisp {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pinky {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    gorger {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dweller {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vorach {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sutiramu {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cinder {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    shade {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    banshee {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    aegis {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    krake {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    erepede {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sylph {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    volcan {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    yale {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    darkling {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    epion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ioray {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    trite {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    jengu {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    roa {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    roc {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ventoraptor {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    salamander {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    calpod {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    remobra {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    concapedesegment {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pixen {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    triffid {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    belph {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    necrovore {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cephignis {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    maka {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    feradon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    xaphan {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    grigori {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    gnekk {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cockatrice {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wraamon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    conba {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ent {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tarantula {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rahovart {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    frostweaver {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ignibus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    argus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tpumpkyn {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    abaia {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    balayang {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


magma_monsters {

    magma_monster {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    magma_monster_grunt {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


minecraft {

    elder_guardian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wither_skeleton {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.3

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    stray {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    husk {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombie_villager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton_horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombie_horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    donkey {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mule {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    evocation_illager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vex {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vindication_illager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    illusion_illager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    creeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.5

        # The factor to scale the entity by. Default: 1
        D:scale=0.8

        # The factor to scale the entity's speed by. Default: 1
        D:speed=0.9

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    giant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombie {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ghast {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombie_pigman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    enderman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cave_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=0.6

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=0.6

        # The factor to scale the entity's speed by. Default: 1
        D:speed=0.8

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    silverfish {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    blaze {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    magma_cube {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wither {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.1

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    witch {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    endermite {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    guardian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    shulker {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pig {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sheep {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    cow {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    chicken {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    squid {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wolf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mooshroom {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    snowman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ocelot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    villager_golem {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    horse {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rabbit {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    polar_bear {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    llama {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    parrot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    villager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    player {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity's reach distance by. Default: 1
        D:reach=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


mod_lavacow {

    lavacow {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombiemushroom {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    parasite {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    foglet {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombiefrozen {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    undeadswine {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    salamander {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ithaqua {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mimic {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sludgelord {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lilsludge {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    raven {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ptera {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=0.5

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vespa {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    scarecrow {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    vespacocoon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    piranha {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    zombiepiranha {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    boneworm {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pingu {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    undertaker {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    unburied {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ghostray {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    banshee {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    weta {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    avaton {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    forsaken {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeletonking {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mummy {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


mowziesmobs {

    foliaath {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    baby_foliaath {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ferrous_wroughtnaut {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    barakoan_barakoana {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    barakoana {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    barakoaya {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    barakoan_player {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    barako {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    frostmaw {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    grottol {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lantern {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    naga {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


multimob {

    dummy {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


netherex {

    pigtificate_leader {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pigtificate {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    gold_golem {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mogus {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    salamander {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wight {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ember {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    nethermite {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spinout {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spore_creeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    spore {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ghastling {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    coolmar_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    brute {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    frost {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ghast_queen {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


outfox {

    fox {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


primal {

    ovis_atre {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    canis_campestris {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bear_brown {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bear_black {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    creeper_nether {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    gator {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    shark_hammerhead {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


primitivemobs {

    chameleon {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    treasure_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    haunted_tool {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    grovesprite {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bewitched_tome {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    filch_lizard {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    brain_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rocket_creeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    festive_creeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    support_creeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton_warrior {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    blazing_juggernaut {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lily_lurker {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mother_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    baby_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    trollager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lost_miner {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    traveling_merchant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dodo {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mimic {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    sheepman {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    goblin {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    harpy {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    flame_spewer {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    void_eye {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


pyrotech {

    pyrotech {

        mud {
            # The amount of extra armor to give the entity. Default: 0
            D:armor=0.0

            # The factor to scale the entity's damage by. Default: 1
            D:damage=1.0

            # The factor to scale the entity's follow range by. Default: 1
            D:followRange=1.0

            # The factor to scale the entity's max health by. Default: 1
            D:health=1.0

            # The factor to scale the player's jump strength by. Default: 1
            D:jump=1.0

            # The factor to scale the entity by. Default: 1
            D:scale=1.0

            # The factor to scale the entity's speed by. Default: 1
            D:speed=1.0

            # The amount of extra armor toughness to give the entity. Default: 0
            D:toughness=0.0
        }

    }

}


quark {

    stoneling {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    archaeologist {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    foxhound {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    dweller {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ashen {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pirate {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    crab {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wraith {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    leash_knot {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


randomthings {

    spirit {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    goldenchicken {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


rats {

    rat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    illager_piper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ratlantean_spirit {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    marbled_cheese_golem {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    feral_ratlantean {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    neo_ratlantean {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pirat_boat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pirat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    plague_doctor {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    black_death {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    plague_cloud {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    plague_beast {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


roughmobsrevamped {

    bosszombie {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bossskeleton {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hostilebat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


toroquest {

    toroquest_mage {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_monolitheye {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_village_lord {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_guard {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_shopkeeper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_bas {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_vampire_bat {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_sentry {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_fugitive {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_toro_villager {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_pig_lord {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_zombie_raider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_zombie_villager_raider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_spider_lord {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_wolf_raider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_grave_titan {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_bandit_lord {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_caravan {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_orc {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_construct {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    toroquest_construct_quest {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


trumpetskeleton {

    trumpet_skeleton {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


twilightforest {

    wild_boar {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bighorn_sheep {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    deer {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    redcap {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    swarm_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    skeleton_druid {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hostile_wolf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    wraith {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hedge_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lich {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=2.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    penguin {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    lich_minion {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    loyal_zombie {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tiny_bird {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    squirrel {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    bunny {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    raven {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    quest_ram {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    kobold {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mosquito_swarm {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    death_tome {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    minotaur {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    minoshroom {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    fire_beetle {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    slime_beetle {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    pinch_beetle {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    maze_slime {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    redcap_sapper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mist_wolf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    king_spider {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    firefly {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    mini_ghast {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tower_ghast {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tower_golem {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tower_termite {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    tower_broodling {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ur_ghast {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    goblin_knight_upper {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    goblin_knight_lower {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    helmet_crab {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    knight_phantom {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    yeti {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    yeti_alpha {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    winter_wolf {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    snow_guardian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    stable_ice_core {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    unstable_ice_core {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    troll {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    giant_miner {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    armored_giant {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    ice_crystal {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    harbinger_cube {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    adherent {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    roving_cube {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    castle_guardian {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    hydra_head {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    boggard {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

    rising_zombie {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


zombie_players {

    zombie_player {
        # The amount of extra armor to give the entity. Default: 0
        D:armor=0.0

        # The factor to scale the entity's damage by. Default: 1
        D:damage=1.0

        # The factor to scale the entity's follow range by. Default: 1
        D:followRange=1.0

        # The factor to scale the entity's max health by. Default: 1
        D:health=1.0

        # The factor to scale the player's jump strength by. Default: 1
        D:jump=1.0

        # The factor to scale the entity by. Default: 1
        D:scale=1.0

        # The factor to scale the entity's speed by. Default: 1
        D:speed=1.0

        # The amount of extra armor toughness to give the entity. Default: 0
        D:toughness=0.0
    }

}


